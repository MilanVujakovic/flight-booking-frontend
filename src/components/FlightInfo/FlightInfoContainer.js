import React, { Component } from 'react';
import FlightInfo from "./FlightInfo";

export class FlightInfoContainer extends Component {
    render() {
        return (
            <FlightInfo />
        )
    }
}

export default FlightInfoContainer
